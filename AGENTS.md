# Hunyuan Pro 仓库协作规范

## 范围与证据

- 用户明确提出的目标就是当前工作边界；未经确认，不得把局部任务扩大为整套路线图、发布或重构。
- 修改代码或汇报状态前，必须区分：已关闭且可复用的证据、本次代码或契约变化、明确不在范围内的工作。
- 底层契约未变化时，应复用既有验收证据，只验证本次变化实际影响的最小范围。
- 架构或迁移任务以 `docs/architecture-baseline/` 中最新的关闭和验收记录为准；历史设计文档不得覆盖后续关闭结论。

## 变更纪律

- 工作区存在无关改动时必须保留，不得擅自回退、格式化、暂存或并入当前任务。
- 修改必须保持在受影响的所有权边界内，优先遵循现有模式；只有确实降低复杂度时才引入新抽象或依赖。
- 新增或改造的复杂、关键代码必须补充中文注释，说明业务原因、约束、兼容分支、权限或数据边界，而不是逐行复述显而易见的代码。
- 新增和修改的文本文件必须使用 UTF-8；不得为了格式化而无关地整文件转码。
- 需求范围清楚后直接执行，不重复发起设计或审批循环；只有所需动作会实质扩大范围或改变既有契约时，才先说明边界并请求决策。

## 验证与汇报

- 验证强度应与改动面匹配。仅因依赖方客户端变化，不得重复运行整个子系统；除非该子系统的契约或持久化数据也发生变化。
- 未满足请求的验收条件时不得报告完成；必须指出确切的剩余门槛。
- 进度汇报先给结论。涉及百分比或完成度时，必须说明分母、纳入和排除范围、已验证证据及剩余工作。

用户指令优先于本文件。
